"use client";

import Governance from "./index";

export default function GovernancePage() {
  return (
    <Governance
      miw={{xs: "100%", lg: "0"}}
      maw={{xs: "100%", lg: "48%"}}
      withBorder
    />
  );
}